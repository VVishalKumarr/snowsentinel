Drop real satellite imagery (or NASA/Copernicus GIBS tile exports) here to
replace the synthetic SVG renders currently produced by
`lib/satelliteArt.ts` and `components/SatelliteViewer.tsx`. See the root
README section "Connecting real satellite data" for the swap points.
